const Profile = ({ name, age, job }) => {
    return (
        <div>
            <h3>Name: {name}</h3>
            <p>Age: {age}</p>
            <p>Job: {job}</p>
        </div>
    )
}

export default Profile